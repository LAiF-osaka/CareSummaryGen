"""プロンプトテンプレートモジュール。

Agentic Search の各ノードで使用するプロンプトテンプレートを定義する。
現行 app.py の MAP/REDUCE プロンプトを移植しつつ、
テンプレート駆動の Agentic Search 用に再設計している。
"""

# --- plan ノード用 ---
PLAN_PROMPT = """あなたは看護サマリー作成の専門家です。
以下のテンプレートセクションに必要な情報を医療記録から探すための検索キーワードを生成してください。

## 対象セクション
名前: {section_name}
説明: {section_description}

## 医療記録の日付範囲
{available_dates}

## 指示
- このセクションを完成させるために医療記録を検索するキーワードを3〜5個生成する。
- キーワードは医療記録の本文に実際に出現しそうな短い単語・語句にする（例: 「バイタル」「SpO2」「服薬指導」）。
- 文章・表・見出し・記号は禁止。各キーワードは20文字以内。

## 出力形式
必ず次の JSON のみを出力する。説明文・コードフェンス・表は一切含めない。
{{"queries": ["キーワード1", "キーワード2", "キーワード3"]}}
"""

# --- search ノード用 ---
SEARCH_PROMPT = """あなたは医療記録の検索エージェントです。
セクション「{section_name}」に必要な情報を見つけるため、
検索ツールを使って医療記録から探してください。

探すべき情報: {search_description}
日付範囲: {date_range}
チャンク数: {chunk_count}

利用可能な検索ツール:
- search_by_keyword(keyword): キーワードで記録を検索
- search_by_date_range(start_date, end_date): 日付範囲で検索（YYYYMMDD形式）

どのツールをどのパラメータで呼び出すか決定してください。
複数のツールを並列で呼び出すことができます。
"""

# --- extract ノード用 ---
EXTRACT_PROMPT = """以下の医療記録の抜粋から、指定されたセクションに必要な情報を抽出してください。

## 対象セクション
名前: {section_name}
説明: {section_description}

## 医療記録の抜粋
{search_results}

## 指示
- 具体的な数値（バイタルサイン、投薬量等）を必ず含めてください
- 日付を YYYY年MM月DD日 形式で記載してください
- 記録にない情報は「記録なし」と明記してください
- 推測や憶測は含めないでください
- 医学的略語は初出時にフルスペルを併記してください
"""

# --- evaluate ノード用 ---
EVALUATE_PROMPT = """以下はあるセクションに対して抽出された情報です。
このセクションの要件を満たすのに十分な情報が揃っていますか？

## セクション: {section_name}
## 要件: {section_description}

## 抽出済み情報
{extracted_info}

## 指示
情報が十分であれば「SUFFICIENT」とだけ回答してください。
不足している場合は、追加で検索すべき具体的なキーワードを箇条書きで回答してください。
"""

# --- synthesize ノード用 ---
SYNTHESIZE_PROMPT = """以下のセクション別に抽出された情報を統合し、
テンプレートに沿った看護サマリーのドラフトを作成してください。

## テンプレート
{format_instruction}

## セクション別抽出情報
{section_data}

## 指示
- 各セクションの情報を統合し、時系列順に整理してください
- バイタルサイン・投薬量等の具体的数値を必ず含めてください
- 情報がないセクションは「記録なし」としてください
- 推測は含めず、抽出された事実のみを記載してください
- 医学的略語は初出時にフルスペルを併記してください
"""

# --- reflect ノード用 ---
REFLECTION_PROMPT = """あなたは看護サマリーの品質管理担当者です。
以下の看護サマリーを批評し、具体的な問題点があれば指摘してください。

## 確認観点
- バイタルサイン・投薬量に具体的な数値が記載されているか
- 日付が時系列順に整理されているか
- 指定フォーマットの全セクションが埋まっているか
- 推測・憶測が混入していないか（客観的事実のみか）
- 医療略語が初出時に正式名称で展開されているか

## 対象テンプレートのセクション
{template_sections}

## 看護サマリー
{draft_summary}

## 出力形式
問題がある場合:
改善すべき具体的な箇所を箇条書きで列挙してください。
各指摘には「どのセクションの」「何が」「どう問題か」を明記してください。

問題がない場合:
「APPROVED」とだけ回答してください。
"""

# --- revise ノード用 ---
REVISION_PROMPT = """以下の看護サマリーを、指摘された問題点に基づいて改善してください。

## 元のサマリー
{draft_summary}

## 指摘された問題点
{feedback}

## 指示
- 指摘された箇所のみを修正してください
- 正しい部分は変更しないでください
- 修正後のサマリー全体を出力してください
"""
